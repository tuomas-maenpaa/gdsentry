# GDSentry UI Test Template
# Test class: {{CLASS_NAME}}

extends GDSentry.UITest

class_name {{CLASS_NAME}}UITest

# ------------------------------------------------------------------------------
# TEST SUITE SETUP
# ------------------------------------------------------------------------------
func _ready():
	test_description = "UI tests for {{CLASS_NAME}}"
	test_tags = ["ui", "interaction"]
	test_priority = "medium"

# ------------------------------------------------------------------------------
# TEST SUITE
# ------------------------------------------------------------------------------
func run_test_suite() -> void:
	# Run all {{CLASS_NAME}} UI tests
	run_test("test_{{METHOD_NAME}}_ui", func(): return test_{{METHOD_NAME}}_ui())

# ------------------------------------------------------------------------------
# INDIVIDUAL TESTS
# ------------------------------------------------------------------------------
func test_{{METHOD_NAME}}_ui() -> bool:
	# Test {{METHOD_NAME}} UI interaction
	# Find UI elements
	var button = find_button_by_text("Test Button")

	if not button:
		return assert_true(false, "Test button not found")

	# Test interaction
	return click_button(button, true)

# ------------------------------------------------------------------------------
# CLEANUP
# ------------------------------------------------------------------------------
func _exit_tree() -> void:
	# Cleanup test resources
	pass
